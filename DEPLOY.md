# Render 배포
1. GitHub 저장소 최상단에 `public`, `server.js`, `package.json`, `Dockerfile`, `render.yaml` 등이 바로 보이게 업로드합니다.
2. Render의 Root Directory는 비워 둡니다.
3. Manual Deploy → Deploy latest commit을 실행합니다.
4. Deploy succeeded가 뜨면 기존 onrender.com 주소로 접속합니다.
