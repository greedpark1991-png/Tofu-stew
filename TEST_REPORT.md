# 병맛 동물 격투게임 REBOOT — TEST REPORT

## 테스트 범위

이번 테스트는 기존 프로젝트의 수정본이 아니라 새로 만든 REBOOT 프로젝트 전체를 대상으로 진행했습니다.

확인 목표는 다음과 같습니다.

- 찌개 / 만두 / 감자 / 구찌 4마리 외의 플레이어 캐릭터가 존재하지 않는가
- 로봇 / 마네킹 / 회색 네모 / 원형 / humanoid placeholder가 최종 화면에 노출되지 않는가
- 정식 원본 PNG가 프로젝트 안에 보존되어 있는가
- 런타임 캐릭터가 정식 원본 PNG 기반인가
- 공격이 분리된 팔 / 다리 그래픽이 아니라 캐릭터 전체 몸 모션으로 표현되는가
- 3 Stock + 누적 데미지 % + 장외 규칙이 동작하는가
- 6종 아이템과 4종 맵이 코드에 존재하는가
- 실제 브라우저 환경에서 홈 → 선택 → 전투 → 입력 → 피격 → 일시정지 흐름이 오류 없이 동작하는가

## 1. 자동 정적/구조 테스트

명령:

```bash
npm test
```

결과:

```text
15 passed, 0 failed
```

통과 항목:

1. 찌개 런타임 PNG 존재 / 비어 있지 않음
2. 찌개 원본 PNG 보존
3. 만두 런타임 PNG 존재 / 비어 있지 않음
4. 만두 원본 PNG 보존
5. 감자 런타임 PNG 존재 / 비어 있지 않음
6. 감자 원본 PNG 보존
7. 구찌 런타임 PNG 존재 / 비어 있지 않음
8. 구찌 원본 PNG 보존
9. humanoid / robot / mannequin 스프라이트 코드 없음
10. 3 Stock 규칙 존재
11. 누적 데미지 % 증가 규칙 존재
12. 요청한 6종 아이템 존재
13. 생활형 4종 맵 존재
14. 전투 렌더링이 캐릭터 전체 PNG 기반
15. 분리된 팔 / 다리 공격 스프라이트 렌더링 없음

## 2. JavaScript 문법 검사

명령:

```bash
node --check public/game.js
```

결과: PASS

문법 오류 없이 통과했습니다.

## 3. 브라우저 스모크 테스트

Headless Chromium에서 실제 DOM / Canvas 로직을 구동해 아래 흐름을 확인했습니다.

- 홈 화면 로딩: PASS
- 캐릭터 선택 화면 이동: PASS
- 4개 캐릭터 카드 표시: PASS
- 4개 맵 카드 표시: PASS
- 선택 캐릭터 대형 프리뷰: PASS
- 전투 시작: PASS
- 플레이어 + BOT 3명, 총 4마리 생성: PASS
- `D`, `J`, `K`, `Space` 입력 처리: PASS
- 실제 공격 판정으로 상대 누적 데미지 증가 확인: PASS
- `ESC` 일시정지 표시: PASS
- Console error: 0
- Page error: 0

샌드박스 브라우저는 로컬 HTTP 주소에 직접 접근하는 데 제한이 있어, 동일 HTML/CSS/JS와 PNG를 한 문서에 로드하는 self-contained 테스트 하네스로 검증했습니다. 최종 프로젝트 자체는 일반적인 로컬 정적 서버 구조이며 `npm start`로 실행합니다.

## 4. 시각 검수

`docs/`에 실제 렌더링 캡처를 포함했습니다.

- `preview_home.png`
- `preview_select.png`
- `preview_game.png`
- `preview_action.png`

확인 결과:

- 시작 화면부터 4마리가 모두 보이며 사람형 더미가 없음
- 선택 화면에서 선택 캐릭터가 크게 표시됨
- 찌개 / 만두 / 감자 / 구찌의 실루엣과 색이 서로 구분됨
- 감자는 의도적으로 가장 작게 렌더링되어 아기 강아지 느낌을 유지함
- 인게임에서도 4마리 PNG만 캐릭터로 보임
- 공격 / 점프 / 대시는 캐릭터 전체 PNG를 squash / stretch / tilt / lunge 하는 방식
- 히트박스는 화면에 그려지지 않음
- 생활형 맵 오브젝트와 플랫폼이 테마에 맞게 보이도록 구성됨

## 5. 현재 남은 개선점

이번 버전은 귀여운 원본 디자인 보존을 최우선으로 잡았기 때문에, 원본과 생김새가 달라지는 AI 재드로잉을 억지로 넣지 않았습니다. 그 결과 공격 애니메이션은 현재 캐릭터 전체 PNG의 변형 모션 중심입니다.

향후 퀄리티를 더 올리려면 다음이 가장 효과적입니다.

1. 원본 얼굴 / 귀 / 체형을 정확히 유지한 캐릭터별 2~4프레임 전용 공격 포즈 추가
2. 걷기 / 피격 / 장외 표정을 캐릭터별로 조금 더 세분화
3. 타격음 / 장외음 / 맵별 BGM을 실제 음원 에셋으로 교체
4. BOT 난이도 단계와 공격 성향 차별화
5. 로컬 2P~4P 입력 지원

현재 범위에서는 기능 수를 늘리는 대신 캐릭터가 화면에서 원본처럼 보이는 것, 감자의 작은 체구, 네 마리만 등장하는 것, 단순 장외 난투의 즉각적인 재미를 우선했습니다.

- Hotfix: 감자/만두 원본 PNG 매칭을 사용자 최종 첨부본 기준으로 재고정하고 runtime sprite + canonical source를 동시 교체함.
